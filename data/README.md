# Data

This directory contains the scripts to fetch and process the data from the sources.

## Development

Run `scripts/secrets-pull.sh` to pull the secrets from the vault.

Run the scripts from the data directory.
